# sci-paper optional runtime dependencies.
#
# The shared feedback schema, deterministic L0 linter, model-free distribution /
# sentence-structure / document-structure axes, and repository validator use the
# Python standard library. Install the packages below only for the corresponding
# optional capabilities. Missing dependencies must remain visible as unmeasured
# or degraded evidence; they must not be replaced by nominal zero findings.

numpy>=1.20                  # learned feature vectors, caches, and rewrite scoring
pymupdf>=1.20                # PDF corpus extraction and compiled-page inspection
sentence-transformers>=2.0   # semantic exemplar retrieval and embedding features
scikit-learn>=1.2            # class_weight support in HistGradientBoostingClassifier
joblib>=1.0                  # local model-bundle serialization
transformers                 # optional token-surprisal / UID measurement
torch                        # tensor/model runtime used by deai_oracle.py
