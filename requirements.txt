# sci-paper plugin runtime dependencies.
#
# stdlib alone is enough to run:
#   - tools/ai_ism_lint.py (regex rules + corpus blacklist)
#   - tools/extract_style.py on .tex/.txt corpora (no PDF, no exemplar ML)
#
# Below are the deps for the full v0.3 pipeline. Each is gracefully
# degraded if missing — see tools/README.md for the fallback behaviour.

numpy>=1.20                  # used everywhere; sentence-transformers / sklearn dep
pymupdf>=1.20                # PDF corpus parsing in extract_style.py
sentence-transformers>=2.0   # embedding-based retrieval in retrieve_exemplars.py
scikit-learn>=1.0            # AI-ism classifier in train_ai_ism_classifier.py
joblib>=1.0                  # classifier serialization (already a sklearn dep)
