# Generated style artifacts (regenerate from corpus via tools/extract_style.py).
# Match at any depth so per-field dirs (style-profile/<field>/...) are covered.
style-profile/**/*.json
style-profile/**/*.jsonl
style-profile/**/*.csv
style-profile/**/style_dossier.md
# Embedding caches built by retrieve_exemplars.py (per model name).
style-profile/**/exemplar_embeddings_*.npy
# Trained model artifacts + feature caches (regenerate per-field via the
# train_*.py tools + --refeature). The plugin ships the tools + skills; each
# field rebuilds its own from its (also-ignored) corpus — so voice models built
# on unpublished drafts never leak. Covers ai_ism_classifier.joblib,
# voice_model.joblib, and voice_features_cache.npz.
style-profile/**/*.joblib
style-profile/**/*.npz
# Auto-extracted negative-sample text (contains user's project file content;
# privacy-sensitive — do NOT commit). Regenerate via extract_md_negatives.py.
style-profile/**/ai_ism_negatives_extracted.txt
# Generated document-level validation sets (AI-drafted + de-AI-rewritten .tex used to
# validate the document-dispersion detector). Evidence, not product; regenerate on demand.
style-profile/**/docval/

# Corpus content (copyright-sensitive — DO NOT commit).
# Blanket-deny everything under tier dirs (PDFs, .tex, .bib, .eps, .ps, .cls,
# .bbl, .sty, .bst, figure PDFs, arxiv source bundles, …) so a new corpus
# format can't accidentally leak.
style-corpus/**/tier-*/**
# Bulk full-text arXiv paper sources (fetch_arxiv_abstracts.py --fulltext);
# same copyright treatment as the tier dirs. The glob covers the whole class,
# not just the calibration pull: --fulltext-dir names a new fulltext-* directory
# per evaluation set, and naming them one at a time here meant every new
# held-out set started life as a tracked, publishable copy of other people's
# papers. One rule, whole class.
style-corpus/**/fulltext-*/
# Re-include only structural placeholders so the layout is documented in git.
!style-corpus/**/tier-*/.gitkeep
!style-corpus/**/tier-*/README.md
# Field-root and corpus-root README's are tracked by default.
!style-corpus/README.md
!style-corpus/**/README.md

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.pytest_cache/

# Personal / machine-specific Claude Code project memory.
# CLAUDE.md is shared (working principles); CLAUDE.local.md is for paths,
# virtualenvs, machine quirks — never commit it.
CLAUDE.local.md

# cc-memory plugin local state (per-machine sqlite + private conversation
# notes + topic/session indexes). Regenerable; never commit.
memory/

# Local snapshots taken before a profile rebuild or recalibration. They hold
# copies of the same corpus-derived, copyright-sensitive artifacts the
# style-profile rules above exclude, so they carry the same never-commit
# treatment. They live HERE rather than beside the repository: a sibling
# directory under the projects root reads as a project of its own.
.backups/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
.ce/

# Build artefacts from any test runs
*.aux
*.log
*.out
*.synctex.gz
# HuggingFace hub entries relocated from the user cache (junction back)
models/hf-cache/
# cc-enforcer index / cc-memory state: machine-local, never content
.ccm/
