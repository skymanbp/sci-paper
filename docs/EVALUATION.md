# Evaluation record — moved

The canonical, authoritative evaluation record for the de-AI subsystem is the
repository-root [`../EVALUATION.md`](../EVALUATION.md). This stub exists only
so old links resolve; it intentionally carries no numbers, tables, or claims,
keeping a single data source.

Normative policy lives in [`SCIPAPER_STANDARD.md`](SCIPAPER_STANDARD.md);
generated per-field measurements live in
`style-profile/<field>/voice_model_evaluation.json`.
